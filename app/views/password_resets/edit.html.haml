%h1.heading
  = I18n.t("set_your_password")
- form_for @user, :url => password_reset_path, :method => :put do |f|
  = f.error_messages
  = render :partial => "password_resets/form", :locals => {:f => f}
  %div
    = submit_tag I18n.t("update_password"), :class => "submit"

